import PreviewPage from "@/components/orders/preview/PreviewPage";

export default function PreviewOrdersPage({ params }: { params: { storeId: string; }; }) {
    return (

        <PreviewPage />

    );
}
