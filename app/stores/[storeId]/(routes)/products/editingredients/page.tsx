import Editngredients from '../../../../../../components/products/catalog/EditIngredients';

export default function AddIngredientsPage({
  params,
}: {
  params: { storeId: string }
}) {
  return <Editngredients />
}
