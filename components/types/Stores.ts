export interface StoreWidgetProps {
    name: string;
    location: string;
    personCount: number;
    logoUrl: string;
  }
export interface StoreInfo {
  store: string;
  supplier: string;
  salse: string;
  budget: string;
  shipDate: string;
  purchaseDate: string;
}
